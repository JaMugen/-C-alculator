#include <stdio.h>

int main(void)
{
    long int first_opperand;
    printf("Enter the opparand, then the operator, and lastly the final opperand to get answer. \n");
    scanf("%d", &first_opperand);

    return 0;
}
